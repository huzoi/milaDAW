//Primer he provat a fer el programa a la meva manera i va quedar així.
/*

#include <stdio.h>

void main()
{
int edat

printf ("Quants anys tens?)
scanf ("%d%, &edat)

if (edat >18 true) printf ("Ets major d'edat!");

if (edat >18 false) printf ("Ets menor d'edat!!!!");

getch ();

{

*/

//Obviament no em va funcionar, així que vaig buscar cóm funcionaba la funció 'if', l'havia escrit malament, però tot i aixo, em vaig sorprendre cóm sense tenir-ne idea m'hi vaig apropar.


#include <stdio.h>


void main()

    {

    int edat;

    printf ("Quants anys tens???");
    scanf ("%d", &edat);

    if (edat >= 18)
      {
        printf ("Ets major d'edat! ja pots anar a la preso");
      }

    if (edat <= 18)
      {
        printf ("Ets menor d'edat! Ves a casa a mirar el pocoyo");
      }

    getch();

    }
